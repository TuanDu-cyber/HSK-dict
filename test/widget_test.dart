import 'package:flutter_test/flutter_test.dart';
import 'package:hsk_dict_app/app.dart';

void main() {
  test('HskDictApp can be constructed', () {
    expect(const HskDictApp(), isA<HskDictApp>());
  });
}
